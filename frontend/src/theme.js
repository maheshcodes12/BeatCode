import { createTheme } from "@mui/material";

export const theme = createTheme({
	palette: {
		primary: {
			main: "#fff",
			light: "#fff",
		},
		secondary: {
			main: "#F5F5F5",
			light: "#F5F5F5",
		},
		otherColor: {
			main: "#FFECD0",
			light: "#FFECD0",
		},
	},
});
